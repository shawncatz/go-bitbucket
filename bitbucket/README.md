# bitbucket library

For `bub` to have the same functionality as `hub`, we will need
the following functionality:

[ ] create repo
[ ] browse repo
[ ] compare repo
[ ] pull-request
[ ] fork?
[ ] ci-status?
